<view class="page">
  <!-- Hero卡片 - 设备状态 -->
  <view class="hero-card">
    <view class="device-header">
      <view class="device-info">
        <view class="device-icon">⚖️</view>
        <view class="device-text">
          <text class="device-name">智能体脂秤</text>
          <view class="device-status">
            <view class="status-dot {{scanning ? 'broadcasting' : (device ? 'connected' : 'disconnected')}}"></view>
            <text class="status-text">{{scanning ? '广播中' : (device ? '已连接' : '未连接')}}</text>
          </view>
        </view>
      </view>
      <button class="refresh-btn {{scanning ? 'loading' : ''}}" bindtap="refreshDevices" loading="{{scanning}}" disabled="{{false}}">
        <text wx:if="{{!scanning}}">刷新</text>
        <text wx:else>扫描中</text>
      </button>
    </view>
  </view>

  <!-- 实时体重仪表盘 -->
   <view class="gauge-section" wx:if="{{weight >= 0}}">
     <view class="gauge-card">
       <view class="gauge-viewport">

         <view class="gauge-container">
           <block wx:for="{{61}}" wx:key="index">
             <view class="tick {{index % 10 === 0 ? 'major' : (index % 5 === 0 ? 'mid' : 'minor')}}"
                   style="transform: rotate({{(index * 3) - 90}}deg);"></view>
           </block>

           <view class="gauge-labels">
             <text class="label-item l-0">0</text>
             <text class="label-item l-75">75</text>
             <text class="label-item l-150">150</text>
           </view>
         </view>

         <view class="needle-layer" style="transform: rotate({{(weight / 150) * 180 - 90}}deg);">
           <view class="needle-blade"></view>
         </view>

         <view class="data-hub">
           <view class="weight-box {{isStabilized ? 'stable' : ''}}">
             <text class="weight-value">{{weightDisplay}}</text>
             <text class="weight-unit">kg</text>
           </view>
           <view class="status-pill {{isStabilized ? 'pill-stable' : 'pill-scan'}}">
             <view class="dot"></view>
             <text>{{isStabilized ? '测量完成' : '实时测量中'}}</text>
           </view>
         </view>

       </view>
     </view>
   </view>

  <!-- 家庭成员选择 - 只在测量完成后显示 -->
  <view class="member-section {{isStabilized && impedance > 0 ? 'fade-in' : ''}}" wx:if="{{isStabilized && impedance > 0}}">
    <view class="section-header">
      <text class="section-title">家庭成员</text>
      <button class="add-member-btn" bindtap="showAddMemberModal">
        <text class="iconfont">+</text>
      </button>
    </view>

    <!-- 加载中提示 -->
    <view class="loading-members" wx:if="{{isLoadingMembers}}">
      <text class="loading-text">加载中...</text>
    </view>

    <scroll-view class="member-list" scroll-x wx:else>
      <view wx:for="{{members}}" wx:key="id"
            class="member-card {{selectedMemberId === item.id ? 'active' : ''}}"
            bindtap="selectMember"
            bindlongpress="onLongPressMember"
            data-id="{{item.id}}"
            data-name="{{item.name}}">
        <view class="member-avatar" style="background: {{item.avatarColor}}">
          <text class="avatar-text">{{item.name[0]}}</text>
        </view>
        <text class="member-name">{{item.name}}</text>

        <!-- 基本信息 -->
        <view class="member-info" wx:if="{{item.age || item.height}}">
          <text class="info-item" wx:if="{{item.age}}">{{item.age}}岁</text>
          <text class="info-divider" wx:if="{{item.age && item.height}}">|</text>
          <text class="info-item" wx:if="{{item.height}}">{{item.height}}cm</text>
        </view>

        <!-- 最新体重 -->
        <view class="member-stats" wx:if="{{item.lastWeight}}">
          <text class="member-weight">{{item.lastWeight}}kg</text>
        </view>

        <!-- 历史数据趋势图标 -->
        <view class="member-trend" wx:if="{{item.weightHistory && item.weightHistory.length > 1}}">
          <text class="trend-icon">📈</text>
          <text class="trend-count">{{item.weightHistory.length}}次</text>
        </view>
      </view>
    </scroll-view>
  </view>

  <!-- 健康数据统计 -->
  <view class="stats-grid {{currentMember && bodyFat ? 'fade-in' : ''}}" wx:if="{{currentMember && bodyFat}}">
    <view class="stat-card">
      <view class="stat-icon">⚖️</view>
      <view class="stat-content">
        <text class="stat-label">当前体重</text>
        <view class="stat-value">
          <text class="stat-number">{{weight}}</text>
          <text class="stat-unit">kg</text>
        </view>
      </view>
    </view>

    <view class="stat-card">
      <view class="stat-icon">📊</view>
      <view class="stat-content">
        <text class="stat-label">BMI指数</text>
        <view class="stat-value">
          <text class="stat-number">{{bmi}}</text>
        </view>
        <text class="stat-range">正常: 18.5-23.9</text>
      </view>
    </view>

    <view class="stat-card">
      <view class="stat-icon">💪</view>
      <view class="stat-content">
        <text class="stat-label">体脂率</text>
        <view class="stat-value">
          <text class="stat-number">{{bodyFat}}</text>
          <text class="stat-unit">%</text>
        </view>
        <text class="stat-range">{{currentMember.gender === 'male' ? '男: 10-20%' : '女: 18-28%'}}</text>
      </view>
    </view>

    <view class="stat-card">
      <view class="stat-icon">💧</view>
      <view class="stat-content">
        <text class="stat-label">水分</text>
        <view class="stat-value">
          <text class="stat-number">{{water}}</text>
          <text class="stat-unit">%</text>
        </view>
        <text class="stat-range">{{currentMember.gender === 'male' ? '男: 55-65%' : '女: 45-60%'}}</text>
      </view>
    </view>

    <view class="stat-card">
      <view class="stat-icon">🦴</view>
      <view class="stat-content">
        <text class="stat-label">肌肉量</text>
        <view class="stat-value">
          <text class="stat-number">{{muscleMass}}</text>
          <text class="stat-unit">kg</text>
        </view>
      </view>
    </view>

    <view class="stat-card">
      <view class="stat-icon">🥩</view>
      <view class="stat-content">
        <text class="stat-label">蛋白质</text>
        <view class="stat-value">
          <text class="stat-number">{{protein}}</text>
          <text class="stat-unit">%</text>
        </view>
      </view>
    </view>

    <view class="stat-card">
      <view class="stat-icon">🔥</view>
      <view class="stat-content">
        <text class="stat-label">基础代谢</text>
        <view class="stat-value">
          <text class="stat-number">{{bmr}}</text>
          <text class="stat-unit">kcal</text>
        </view>
      </view>
    </view>

    <view class="stat-card">
      <view class="stat-icon">🫀</view>
      <view class="stat-content">
        <text class="stat-label">内脏脂肪</text>
        <view class="stat-value">
          <text class="stat-number">{{visceralFat}}</text>
        </view>
        <text class="stat-range">正常: 1-9</text>
      </view>
    </view>
  </view>

  <!-- 自动保存提示 -->
  <view class="action-card" wx:if="{{weight > 0 && isStabilized && autoSaved}}">
    <view class="saved-indicator">
      <text class="saved-icon">✓</text>
      <text class="saved-text">已自动保存到数据库</text>
    </view>
  </view>

  <!-- 健康建议 -->
  <view class="advice-card" wx:if="{{currentMember && currentMember.advice}}">
    <view class="card-header">
      <text class="card-title">健康建议</text>
    </view>
    <view class="advice-content">
      <text class="advice-text">{{currentMember.advice}}</text>
    </view>
  </view>

  <!-- 添加/编辑成员弹窗 -->
  <view wx:if="{{showAddMemberDialog}}" class="modal-mask" bindtap="closeAddMemberModal">
    <view class="modal-box modal-form" catchtap="stopPropagation">
      <view class="modal-title">{{editingMemberId ? '编辑成员' : '添加家庭成员'}}</view>

      <view class="form-group">
        <text class="form-label">姓名 <text class="required">*</text></text>
        <input class="form-input" placeholder="请输入姓名" value="{{newMemberName}}" bindinput="onMemberNameInput" />
      </view>

      <view class="form-row">
        <view class="form-group half">
          <text class="form-label">年龄 <text class="required">*</text></text>
          <input class="form-input" type="number" placeholder="必填" value="{{newMemberAge}}" bindinput="onMemberAgeInput" />
        </view>
        <view class="form-group half">
          <text class="form-label">身高(cm) <text class="required">*</text></text>
          <input class="form-input" type="number" placeholder="必填" value="{{newMemberHeight}}" bindinput="onMemberHeightInput" />
        </view>
      </view>

      <view class="form-group">
        <text class="form-label">性别 <text class="required">*</text></text>
        <picker mode="selector" range="{{['男', '女']}}" value="{{newMemberGenderIndex}}" bindchange="onMemberGenderChange">
          <view class="picker-value">{{newMemberGender || '请选择性别'}}</view>
        </picker>
      </view>

      <button class="submit-btn" bindtap="submitAddMember">{{editingMemberId ? '保存修改' : '确认添加'}}</button>
    </view>
  </view>
</view>
